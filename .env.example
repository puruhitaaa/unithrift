# Since .env is gitignored, you can use .env.example to build a new `.env` file when you clone the repo.
# Keep this file up-to-date when you add new variables to \`.env\`.

# This file will be committed to version control, so make sure not to have any secrets in it.
# If you are cloning this repo, create a copy of this file named `.env` and populate it with your secrets.

# The database URL is used to connect to your Supabase database.
POSTGRES_URL="postgres://postgres.[USERNAME]:[PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres?workaround=supabase-pooler.vercel"


# You can generate the secret via 'openssl rand -base64 32' on Unix
# @see https://www.better-auth.com/docs/installation
AUTH_SECRET='supersecret'

# Production URL for your Next.js deployment (used for OAuth redirects)
NEXT_PUBLIC_PRODUCTION_URL='https://unithrift-nextjs.vercel.app'

# Preconfigured Google OAuth provider, works out-of-the-box
# @see https://www.better-auth.com/docs/authentication/google
AUTH_GOOGLE_ID=''
AUTH_GOOGLE_SECRET=''

# Cloudinary
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
CLOUDINARY_UPLOAD_PRESET=

# Midtrans Payment Gateway
# Get your keys from: https://dashboard.sandbox.midtrans.com/ (sandbox) or https://dashboard.midtrans.com/ (production)
# Set MIDTRANS_IS_PRODUCTION to "false" for sandbox keys (testing), "true" for production keys (live payments)
MIDTRANS_IS_PRODUCTION=false
MIDTRANS_SERVER_KEY=your_server_key_here
NEXT_PUBLIC_MIDTRANS_CLIENT_KEY=your_client_key_here
EXPO_PUBLIC_MIDTRANS_CLIENT_KEY=your_client_key_here